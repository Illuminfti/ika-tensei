// Ika Tensei v3 - Events module (documentation only)
// All event structs are defined in the modules that emit them.
// See registry.move for all event definitions.
module ikatensei::events {
    // This module is intentionally minimal.
    // Sui requires event structs to be defined in the same module
    // where emit() is called. All events are in registry.move.
}
